import styled from "styled-components";

const ResourceShowDataWrapper = styled.div`
  height: 100%;
  padding: 20px 0;
  display: flex;
  flex-direction: column;
  flex-wrap: wrap;
`;

export default ResourceShowDataWrapper;
