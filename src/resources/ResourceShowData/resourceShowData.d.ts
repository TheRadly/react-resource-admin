import { DataFieldType } from "../../types/InputEventTypes";
interface ResourceShowDataProps {
    values: DataFieldType[];
    emptyLabel: string;
    arrayTitle: string;
}
declare const ResourceShowData: ({ values, emptyLabel, arrayTitle, }: ResourceShowDataProps) => import("react/jsx-runtime").JSX.Element;
export default ResourceShowData;
