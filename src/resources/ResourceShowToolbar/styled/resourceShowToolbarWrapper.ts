import styled from "styled-components";

const ResourceShowToolbarWrapper = styled.div`
  padding-bottom: 20px;
  width: 100%;
  display: flex;
  justify-content: space-between;
  border-bottom: 1px solid var(--clr-borderColor);
`;

export default ResourceShowToolbarWrapper;
