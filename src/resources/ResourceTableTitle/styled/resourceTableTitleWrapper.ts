import styled from "styled-components";

const ResourceTableTitleWrapper = styled.h2`
  width: 100%;
  margin: 0;
  font-weight: 400;
  color: var(--clr-titleFontColor);
`;

export default ResourceTableTitleWrapper;
